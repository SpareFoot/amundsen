  # Copyright Contributors to the Amundsen project.
# SPDX-License-Identifier: Apache-2.0

# Common dependencies (common, frontend, metadata, search) -------------------------------------------------------------
# Python 3.7 is a hard constraint — all versions below must be compatible with Python 3.7.17.

amundsen-common>=0.27.0,<0.28.0
attrs>=19.1.0
boto3==1.17.23
# click 8.1.x: required by Flask 2.2.5. On Python <3.8 pulls in importlib-metadata → zipp.
click>=8.0,<8.2
flasgger==0.9.5
# Flask 2.2.5: last Python 3.7 compatible release.
# Fixes CVE SNYK-PYTHON-FLASK-5490129 (High - Information Exposure).
# Flask 2.3+ requires Python 3.8+.
Flask>=2.2.5,<2.3
Flask-RESTful>=0.3.6
# flask-cors 5.0.0: last Python 3.7 compatible release.
# Fixes SNYK-PYTHON-FLASKCORS-6670412 (Low - Log Injection).
# flask-cors 6.0+ requires Python 3.9+; need to verify which CVEs remain unfixed at 5.0.0.
flask-cors>=5.0.0,<6.0
# itsdangerous 2.1.2+: required by Flask 2.2.5. Remove old <=2.0.1 cap.
itsdangerous>=2.1.2,<3.0
# Jinja2 3.1.6: fixes 5 XSS/template injection CVEs. Python 3.7 compatible.
Jinja2>=3.1.0,<3.2
jsonschema>=3.0.1,<4.0
# marshmallow 3.19.0: last Python 3.7 compatible release.
# SNYK-PYTHON-MARSHMALLOW-14550833 (Medium - Amplification) requires 3.26.2+ (Py3.8+); unfixable.
# marshmallow 3.20+ requires Python 3.8+.
marshmallow>=3.6,<=3.19.0
marshmallow3-annotations>=1.0.0
pytz==2021.1
# requests 2.31.0: last Python 3.7 compatible release. Pinned to max safe version.
# requests 2.32+ (which fixes 2 CVEs) requires Python 3.8+; unfixable on Python 3.7.
requests>=2.25.0,<=2.31.0
requests-aws4auth==1.1.0
statsd==3.2.1
typing==3.6.4
# Werkzeug 2.2.3: last Python 3.7 compatible release.
# Fixes SNYK-PYTHON-WERKZEUG-3319935 (Low) and SNYK-PYTHON-WERKZEUG-3319936 (High - DoS).
# Werkzeug 2.3+ requires Python 3.8+; RCE + 6 Medium CVEs remain unfixable on Python 3.7.
werkzeug>=2.2.3,<2.3
# wheel 0.41.3: last Python 3.7 compatible release.
# Fixes SNYK-PYTHON-WHEEL-3180413 (High - ReDoS).
# SNYK-PYTHON-WHEEL-15053866 (Medium - Dir Traversal) requires 0.46.2+ (Py3.8+); unfixable.
wheel>=0.41.2,<0.42

# Pin transitive deps to latest Python 3.7 compatible versions.
# These are pulled in by click>=8.0 on Python <3.8.
importlib-metadata>=6.7.0,<6.8.0; python_version < "3.8"
zipp>=3.15.0,<3.16.0; python_version < "3.8"
# urllib3: pinned to latest 1.x (Python 3.7 compatible). urllib3 2.x requires Python 3.8+.
urllib3>=1.26.20,<2.0.0
