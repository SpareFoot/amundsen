# Copyright Contributors to the Amundsen project.
# SPDX-License-Identifier: Apache-2.0

neo4j==1.7.6
neotime==1.7.1
cachetools>=4.2.4,<5.0.0
overrides==2.5

# Transitive dep pins moved to requirements-common.txt (importlib-metadata, zipp, urllib3).
# All three services share the same pins via requirements-common.txt to avoid conflicts.
